class Engine{
 canavas=createCanavas(windowWidrth/2,windowHeight/1.5)
 engine=Engine.create();
 world=engine.world;
    
 canavasmouse = Mouse.create(canavas.etc);
canavasmouse=pixelRation=pixelDensity();
};
